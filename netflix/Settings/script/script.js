
/* let imgCarousel = document.getElementById('get-height');
console.log(imgCarousel);
imgCarousel.addEventListener('load', function() {
    // ottieni l'altezza dell'immagine
    var altezza = imgCarousel.getBoundingClientRect().height;
    
    // fai qualcosa con l'altezza (es. assegnala ad un altro elemento)
    console.log(altezza);
  }); */
/* let getHeight = imgCarousel.getBoundingClientRect().height;
console.log(getHeight); */
/* let allCarouselButtons = document.querySelectorAll('.usr-selcet-nn');
console.log(allCarouselButtons);
for (let i = 0; i < allCarouselButtons.length; i++) {
    allCarouselButtons.style.height = getHeight + 'px';
} */


/* let carouselImgs = document.querySelectorAll('.img-fluid');
console.log(carouselImgs);

carouselImgs.forEach(function (image){
    if (window.innerWidth <= 768){
        image.style['margin-top'] = '0';
        image.style['margin-bottom'] = '0';
    }
}
) */